recursive: true
all: true
filename: "mock_{{ .InterfaceName | snakecase }}.go"
dir: "pkg/mocks/{{ base .SrcPackageName }}mocks"
pkgname: "{{ base .SrcPackageName }}mocks"
structname: "{{.InterfaceName}}"
template: testify
packages:
  github.com/vercel/bridge/api/go/bridge/v1:
    config:
      all: false
      include-auto-generated: true
      include-interface-regex: "AdministratorServiceServer"

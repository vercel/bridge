# Django settings
SECRET_KEY=your-secret-key-here
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1,.vercel.app
AUTO_MIGRATE=true

# Database configuration (choose one method)

# Option 1: DATABASE_URL (recommended for production/Vercel)
# Use a hosted PostgreSQL service like Vercel Postgres, Neon, Supabase, etc.
# DATABASE_URL=postgres://user:password@host:5432/dbname

# Option 2: Individual settings
# DB_ENGINE=postgresql
# DB_NAME=userservice
# DB_USER=postgres
# DB_PASSWORD=your-password
# DB_HOST=localhost
# DB_PORT=5432

# Option 3: SQLite (default for local development only - not for Vercel)
# No configuration needed - just leave DB_ENGINE unset

# ============================================
# Vercel Environment Variables
# ============================================
# Set these in your Vercel project settings:
#
# Required:
#   SECRET_KEY        - Django secret key (generate a secure random string)
#   DATABASE_URL      - PostgreSQL connection string (Vercel Postgres, Neon, etc.)
#
# Optional:
#   DEBUG             - Set to "False" for production
#   ALLOWED_HOSTS     - Comma-separated list (includes .vercel.app by default)
#   AUTO_MIGRATE      - Set to "false" to disable auto-migration on startup (default: true)
